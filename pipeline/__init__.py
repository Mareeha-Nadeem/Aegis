"""
pipeline/__init__.py

Initialises the pipeline package and exposes the main pipeline components:
ingestion guard, chunk classifier, prompt assembler, output validator,
and the RAG baseline/hardened variants.
"""
