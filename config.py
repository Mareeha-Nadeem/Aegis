"""
config.py

Global Configuration.

Centralises all project-wide configuration constants and environment-variable
lookups: model paths, FAISS index locations, risk-score thresholds, LLM API
settings, logging verbosity, and any other tuneable parameters used across
the pipeline, training, and evaluation modules.
"""
