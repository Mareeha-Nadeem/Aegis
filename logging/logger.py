"""
logging/logger.py

Centralised Logger.

Configures and exposes a structured logger used across all Aegis modules.
Supports configurable log levels, rotating file handlers (writing to logs/),
and optional JSON-formatted output for downstream log aggregation tools.
"""
